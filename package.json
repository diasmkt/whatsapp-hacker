{
  "name": "wwp-bot",
  "version": "1.0.0",
  "description": "Multi-Tenant WhatsApp SaaS Engine",
  "main": "index.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npx prisma generate --schema=backend/prisma/schema.prisma && npm run build --workspace=frontend",
    "postinstall": "npx prisma generate --schema=backend/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "IRIS",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^6.4.0"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0"
  }
}